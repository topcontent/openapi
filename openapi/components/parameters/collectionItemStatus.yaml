name: status
in: query
description: |
  Retrieve a list of items with the provided status. The following statuses exist:
   - `draft`
   - `in_production`
   - `reviewable`
   - `approved`
schema:
  type: string