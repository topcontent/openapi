type: object
required:
  - order_name
  - items
properties:
#  test:
#    description: Name of the order.
#    type: string
#    maxLength: 255
#    nullable: true
#    pattern: '^[0-9]{6}$'
#    example: Benjamin
#    minItems: 1
#    enum:
#     - 'one'
#     - 'two'
  order_name:
    description: Name of the order.
    type: string
    maxLength: 255
    example: Translate EN to SV
  items:
    type: array
    description: "An array of Items Payloads. Please see the item payloads information for full details of the required parameters."
    minItems: 1
    items:
      type: object
      required:
        - slug
        - language_code
        - quality_code
        - h1
        - min_words
        - template_id
      properties:
        slug:
          description: Item title. For internally storing, can be generic
          type: string
          maxLength: 255
          example: "//TODO"
        language_code:
          description: Source language code.
          type: string
          minLength: 2
          maxLength: 5
          example: SV
        quality:
          description: Quality level.
          type: string
          minLength: 2
          maxLength: 2
          example: business
          enum:
            - 'raw'
            - 'standard'
            - 'business'
        topic_id:
          description: ID of the ordering topic.
          type: integer
          example: 2
        h1:
          description: //TODO
          type: string
          maxLength: 255
          example: BQ
        min_words:
          description: //TODO
          type: integer
          minLength: 5
          maxLength: 5000
          example: 800
        template_id:
          description: //TODO
          type: string
          example: gh2J34kL
        meta_tags:
          description: //TODO
          type: boolean
          example: true
          default: false
        focus_keyword:
          description: //TODO
          type: array
          maxItems: 1
          items:
            type: object
            properties:
              keyword:
                type: string
                maxLength: 255
              match:
                type: string
                enum:
                  - 'exact'
                  - 'near'
            example:
             keyword: 'Jesica Smith'
             match: 'exact'
        sub_keywords:
          description: //TODO
          type: array
          items:
            type: object
            properties:
              keyword:
                type: string
                maxLength: 255
              match:
                type: string
                enum:
                  - 'exact'
                  - 'near'
            example:
             keyword: 'Jesica Smith'
             match: 'exact'
        source_urls:
          description: //TODO
          type: array
          items:
            type: string
            example:
              - 'https://wiki.com'
              - 'https://wiki2.com'
              - 'https://wiki3.com'
        target_url:
          description: //TODO
          type: string
          maxLength: 255
          example: '//TODO'
        links:
          description: //TODO
          type: array
          items:
            type: object
            properties:
              anchor:
                type: string
                maxLength: 50
              url:
                type: string
                maxLength: 255
            example:
             anchor: '//TODO'
             url: '//TODO'
        tone_of_voice:
          description: "Describing the tone of the text examples: 'Informal', 'Friendly', 'Business', 'Formal'. Those 4 are recommended. Those will give best results"
          type: string
          maxLength: 150
          example: '//TODO'
        instructions:
          description: Instructions for contributor to consider when writing. It is strongly encouraged to provide a detailed instructions for the contributor
          type: string
          maxLength: 1000
          example: '//TODO'
        auto_approve:
          description: "Whether to automatically approve an item after they’ve been finished. Default is false If false, completed item will await review and approval by customer for 96 hours"
          type: boolean
          example: false
          default: fasle
        custom_data:
          $ref: ./CustomData.yaml
#          description: "Up to 1K of storage for client-specific data that may be helpful for you to have mapped to this item"
#          type: string
#          example: "{request_id: 87353}"
        return_options:
          description: Contains array. Please see the return options payloads page for full details of the parameters.
          type: array
          items:
            type: object
            properties:
              integration:
                type: string
                enum:
                  - 'callback_url'
              url:
                type: string
                maxLength: 255
              auto_send:
                type: boolean
                default: false
            example:
             integration: 'callback_url'
             url: '//TODO'
             auto_send: true


