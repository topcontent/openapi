type: array
items:
  type: object
  properties:
    unit_type:
      type: string
      description: |
        Available values:
        - `word`
        - `character`
      example: word
    code:
      type: string
      maxLength: 5
      example: es
    name:
      type: string
      example: Spanish (Spain)
#    localized_name:
#      type: string
#      example: Español