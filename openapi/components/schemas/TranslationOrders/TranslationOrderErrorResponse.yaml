type: object
properties:
  status:
    type: string
    description: |
      Status of the reply. Possible values:
        - ```ok``` - successful request;
        - ```error``` - failed request.
    example: "error"
  errors:
    type: array
    description: Array of errors
    items:
      type: array
      example:
        source_language_code:
          - "The source_language_code is required."
        target_language_code:
          - "The target_language_code is required."