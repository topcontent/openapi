type: object
properties:
  errors:
    type: array
    description: Array of errors
    items:
      type: array
      example:
        items.0.source_language_code:
          - "The items.0.source_language_code may only contain letters."
          - "The items.0.source_language_code must be at least 2 characters."
        items.1.source_language_code:
         - "The items.1.source_language_code may only contain letters."
         - "The items.1.source_language_code must be at least 2 characters."
        items.0.target_language_code:
          - "The items.0.target_language_code may only contain letters."
          - "The items.0.target_language_code must be at least 2 characters."
        items.1.target_language_code:
         - "The items.1.target_language_code may only contain letters."
         - "The items.1.target_language_code must be at least 2 characters."
